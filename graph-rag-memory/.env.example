# Optional local package override while graph memory helpers are unreleased.
# Defaults assume hot and hot-demos are sibling repos under the same parent.
HOT_AI_PATH=../../hot/hot/pkg/hot-ai

# Optional deployment settings.
HOT_API_URL=https://api.hot.dev
HOT_API_KEY=
